// Função para exibir a bebida selecionada no display
function selectDrink(drink) {
  document.getElementById('display').innerText = `Selecionado: ${drink}`;
}

// Função para iniciar o reconhecimento de voz
function startVoiceRecognition() {
  // Implemente o código para iniciar o reconhecimento de voz aqui
}

// Função para processar o pagamento
function processPayment() {
  // Implemente o código para processar o pagamento aqui
}
